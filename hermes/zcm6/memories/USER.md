User requires delegated subagents for long/multi-step tasks: dispatch work to subagents rather than doing everything inline in the orchestrator session, to avoid context overflow. After each work task completes, dispatch a separate review subagent. If review finds major issues, re-dispatch the work subagent with fix instructions. This work-then-review delegation pattern is the user's standard for unmanned/yolo sessions.